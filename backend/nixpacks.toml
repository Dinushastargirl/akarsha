[phases.build]
cmds = ["mvn -B -DskipTests clean package"]

[start]
cmd = "java -Xmx300m -XX:+UseSerialGC -jar target/backend-0.0.1-SNAPSHOT.jar"

[variables]
SPRING_PROFILES_ACTIVE = "default"
JAVA_TOOL_OPTIONS = "-Xmx300m -XX:+UseSerialGC"
